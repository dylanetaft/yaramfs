# /etc/kernel/install.conf additions for yaramfs
# Copy this to /etc/kernel/install.conf (or append if it already exists)

layout=other
initrd_generator=yaramfs
uki_generator=ukify

# Path to the yaramfs project root (where prepare.sh lives)
# Adjust this to match your installation location
yaramfs_root=/opt/yaramfs
